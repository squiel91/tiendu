<div class="row">
  <% if (products && products.length > 0) {
    products.forEach(product => { %>
      <div class="col-6 col-md-4">
        <%- include('./product-item.ejs', { product, from: locals.from }) %>
      </div>
    <% })
  } else { %>
    <%= empty %>
  <% } %>
</div>